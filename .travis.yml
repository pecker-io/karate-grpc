language: java
jdk:
  - oraclejdk8
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
install: true
script:
  - mvn install -Dmaven.javadoc.skip=true -B -V -Dmaven.test.skip=true
  - cd karate-grpc-demo
  - pwd
  - ls
  - mvn test
